import { CloneDirDirective } from './clone-dir.directive';

describe('CloneDirDirective', () => {
  it('should create an instance', () => {
    const directive = new CloneDirDirective();
    expect(directive).toBeTruthy();
  });
});
