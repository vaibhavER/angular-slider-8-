import { WidthDirDirective } from './width-dir.directive';

describe('WidthDirDirective', () => {
  it('should create an instance', () => {
    const directive = new WidthDirDirective();
    expect(directive).toBeTruthy();
  });
});
