import { DotNavDirective } from './dot-nav.directive';

describe('DotNavDirective', () => {
  it('should create an instance', () => {
    const directive = new DotNavDirective();
    expect(directive).toBeTruthy();
  });
});
