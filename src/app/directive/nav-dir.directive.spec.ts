import { NavDirDirective } from './nav-dir.directive';

describe('NavDirDirective', () => {
  it('should create an instance', () => {
    const directive = new NavDirDirective();
    expect(directive).toBeTruthy();
  });
});
